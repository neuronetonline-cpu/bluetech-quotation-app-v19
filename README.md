# Bluetech Quotation App

Windows desktop quotation application for Bluetech Computers.

## Run
Install Python 3.11 and dependencies:

    pip install -r requirements.txt

Then run `run_app.bat` or `python quotation_app.py`.

## GitHub Actions
The workflow at `.github/workflows/build.yml` builds `BluetechQuotation.exe` on Windows.
